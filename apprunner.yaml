version: 1.0
runtime: nodejs14
build:
  commands:
    pre-build:
      - npm install --production
    build:
      - npm build:ci

run:
  command: node dist/apps/dadada-server.js
  network:
  port: 8000
  env: APP_PORT
